If your project uses src/ (codeunit, page, table, …), paste the Webshop folder here:

  ZVG-Nonpa/
    app.json
    src/
      codeunit/
      page/
      Webshop/     ← here (your layout)

Control add-in paths must then be:
  src/Webshop/scripts/productViewer.js
  src/Webshop/scripts/startup.js
  src/Webshop/styles/productViewer.css

PIM architecture (Word, download):
https://github.com/ftwo-lab/Testing-/raw/cursor/visual-product-view-6d56/Webshop/PIM-Architecture.docx

If you instead paste Webshop next to app.json (not inside src), use paths without src/:
  Webshop/scripts/productViewer.js

Then Download Symbols and F5.

Tell Me after publish:
- PIM Families → Create default PIM setup
- PIM Marketplaces → set company names for DE AT ES CH NP CZ
- PIM Product Enrichment
- Product Webshop

IDs used: 50351–50355 and 50600–50642.
If an ID is already used, change only that object number.
